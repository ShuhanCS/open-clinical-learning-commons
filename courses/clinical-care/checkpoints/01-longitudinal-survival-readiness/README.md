# APP-1 Checkpoint 1: Longitudinal and survival readiness

This cumulative Week 3 package freezes the accepted reference workspaces from APP-1 Modules 01 through 03. It carries the 20-point phenotype-and-cohort score once and makes survival evidence a noncompensable progression gate.

Reference disposition: `continue with conditions`. Module 04 is permitted for curriculum construction. Clinical use is prohibited.

```powershell
python build_checkpoint.py --self-check
python build_checkpoint.py --target <new-learner-checkpoint>
python build_checkpoint.py --target <new-reference-checkpoint> --reference
python validate_checkpoint.py <checkpoint-directory>
```

The package does not recompute module evidence. It assembles accepted workspaces and verifies every copied byte.
