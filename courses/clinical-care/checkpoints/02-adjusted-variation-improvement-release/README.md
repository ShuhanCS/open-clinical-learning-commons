# APP-1 Week 6 checkpoint: Adjusted variation and feasible improvement

This checkpoint asks whether the accepted technical case is ready for Module 07 clinician leadership review.

## Reference result

The package scores `45.00 of 45.00` once: 25 points from Module 04 and 20 points from Module 05. Module 06 contributes required gates and zero new points. The reference disposition is `continue with conditions`.

The proposed capacity-aware scheduling workflow may enter leadership review. Clinical implementation and model deployment remain prohibited.

## Run

```powershell
python build_checkpoint.py --self-check
python validate_checkpoint.py --self-check
```

Use `python build_checkpoint.py --target <new-folder>` for a learner package or add `--reference` for the completed teaching package. Existing targets are never overwritten.
