# APP-1 Module 04 learner workspace

Use the fixed field-role and adjustment contracts. Complete every prompted record, build the outputs into a new directory, validate the package, and submit the complete workspace.

REPLACE: record the submitted output directory and validation command.
