# APP-1 Module 02 learner workspace

Complete every `REPLACE` item and all four read-only SQL files. Then build `outputs/` from the accepted Synthea SQLite database and validate the completed workspace.

Do not change the source fingerprint, index period, death rule, day-30 landmark, event window, extension seed, site probabilities, source exposure, or source outcome.
