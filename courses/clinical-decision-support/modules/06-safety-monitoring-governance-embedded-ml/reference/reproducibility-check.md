# Reproducibility check

- Evidence builder: `pass`.
- Repeated-build identity: `all 13 generated files match byte for byte`.
- Workspace builder: `pass`.
- Complete validation: `pass`.
- Copied validation: `pass`.
- Deliberate failure routes: `22 rejected routes`.
- Independent human review: `required before alpha`.

## Limits

Computational reproduction confirms that the frozen inputs and fixed code produce the same teaching artifacts. It does not replace independent clinical, patient-safety, model-risk, equity, accessibility, privacy, security, or workflow review.
