# Prototype release

Status: `INCOMPLETE`.

Record the exact upstream manifest, sandbox release, counts, output digest, runtime boundary, design and threshold roles, accepted threshold, and claim limits.
