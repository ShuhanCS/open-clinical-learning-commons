# Gaming and unintended effects

List the timestamp, routing, denominator, workload, safety, adoption, and access failures that could make the evaluation misleading. For each, add a detection and response. `REPLACE`
