# Reproducibility check

REPLACE with module and checkpoint identities, manifest rows, assembled files, deterministic comparison, nested validators, copied validator, overwrite refusal, learner distinction, environment, commands, and independent human reproducer.
