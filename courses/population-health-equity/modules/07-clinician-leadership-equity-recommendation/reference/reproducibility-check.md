# Reproducibility check

- Checkpoint 01 rebuilt through its accepted builder and validator: `pass`.
- Checkpoint 02 rebuilt through its accepted builder and validator: `pass`.
- Immutable manifest rows: `1,301`.
- Candidate files: `1,328`.
- Manifest bytes: `328429`.
- Manifest SHA-256: `ebae232c051fe8b1204b4266aec416f48fe152b4dc5cda06a3ae00171807097b`.
- Reference builds match: `pass`.
- Learner and reference immutable identities match: `pass`.
- Existing target rejection: `pass`.
- Copied validator: `pass`.
- Independent human clean reproduction: `pending before alpha`.

The builder copies both accepted checkpoint packages and both release records byte for byte. Leadership records cannot repair, omit, or replace frozen evidence.
