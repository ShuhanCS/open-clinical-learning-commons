# Age-band and ACS margin method

- Population: `REPLACE`.
- Denominator source: `REPLACE`.
- Standard population: `REPLACE`.
- Age bands: `REPLACE`.
- Crosswalk rows: `REPLACE`.

Explain the exact B01001 cells, estimate sums, 90 percent margin approximation, zero-estimate-cell rule, covariance limit, zero denominators, and the difference between ACS denominator uncertainty and the synthetic rate interval.

REPLACE
