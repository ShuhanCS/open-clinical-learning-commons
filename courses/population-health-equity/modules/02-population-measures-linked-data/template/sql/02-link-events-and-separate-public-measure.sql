-- Link every synthetic tract-age event row to exactly one independently derived denominator.
-- Keep PLACES modeled prevalence in a separate evidence table.

-- REPLACE with executable SQL that creates linked_synthetic_events and public_modeled_prevalence.
