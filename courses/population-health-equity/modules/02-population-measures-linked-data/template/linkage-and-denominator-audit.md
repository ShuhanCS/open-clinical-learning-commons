# Linkage and denominator audit

Report the complete source union, source-presence states, eligible intersection, denominator rows, synthetic rows, unmatched rows, denominator mismatches, conservation totals, and query-check status.

Explain why the PLACES adult population field remains attached to its modeled estimate rather than replacing the ACS denominator.

REPLACE
