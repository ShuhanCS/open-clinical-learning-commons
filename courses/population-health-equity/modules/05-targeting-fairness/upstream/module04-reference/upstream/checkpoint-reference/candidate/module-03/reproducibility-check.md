# Reproducibility check

- Handoff verification: `pass`.
- Two independent Module 02 freezes match: `pass`.
- Synthetic equity source verification: `pass`.
- Two synthetic equity builds match: `pass`.
- Disparity output verification: `pass`.
- Two disparity builds match: `pass`.
- SQL and independent Python findings match: `pass`.
- All 36 query checks pass: `pass`.
- All 12 source reconciliation checks pass: `pass`.
- All 4,791 complementary suppression audits pass: `pass`.
- Two reference workspaces match: `pass`.
- Nonempty target refusal: `pass`.

The release uses Python's standard library and SQLite. All gzip outputs use a zero modification time. Manifests pin bytes and SHA-256 values. Regeneration cannot repair or change the frozen Module 02 handoff.
