# Reproduction record

REPLACE: record operating system, Python version, package versions, assembly command, validation command, module roots, and output target.

REPLACE: record assembled file count, manifest row count, manifest bytes and SHA-256, two-build result, existing-target refusal, validator check count, and all differences.

REPLACE: identify the accountable reproducer and distinguish checkpoint assembly from module-level analytic reproduction.
