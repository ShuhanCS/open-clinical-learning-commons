# Reproducibility check

REPLACE: record environment, install result, rebuild command and target, upstream verification, invariant-check result, validator result, and any difference from the accepted outputs.

REPLACE: confirm that the builder refused an existing target and that reference outputs were not overwritten.

REPLACE: record whether the optional paired R reading ran in a named environment; a pending R route does not change the Python reference.
