# DAG narrative

REPLACE: narrate all nine nodes and 18 edges in `dag.mmd`, including timing, measured confounders, unmeasured clinical preference, the mediator, the selection node, and the adjustment implication.

REPLACE: state why a structured CSV route is equivalent for a reader who cannot use the rendered graph.

REPLACE: state which arrows are teaching assumptions and what evidence a real study would need.
