# Accessibility review

Every test, fixture, rejection code, verification, claim decision, and summary is available as CSV, JSON, or Markdown. Status never depends on color or an image. The accessible summary uses headings and a simple table; exact evidence remains in adjacent structured outputs. Commands require no pointer interaction.
