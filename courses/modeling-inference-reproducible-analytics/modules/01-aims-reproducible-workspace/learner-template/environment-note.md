# Environment note

REPLACE: Record operating system, architecture, shell, Python executable, Python version, exact installed package versions, encoding, and line-ending behavior.

REPLACE: List the ordered setup, build, and validation commands using repository-relative paths.

REPLACE: Note any difference from the declared pins and the disposition of that difference.
