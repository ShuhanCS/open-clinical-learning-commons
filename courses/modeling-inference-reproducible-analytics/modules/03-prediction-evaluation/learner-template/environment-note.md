# Environment note

REPLACE: record operating system, Python version, NumPy version, pandas version, scikit-learn version, encoding, command history, seed, and bootstrap replicate count.

REPLACE: state whether the pinned environment was used and explain any difference before interpreting output changes.
