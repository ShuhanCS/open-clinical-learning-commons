# Reproducibility check

## Commands

REPLACE: record the exact build and validation commands.

## Results

REPLACE: state whether a new output target reproduced all evidence and whether an existing-target overwrite was refused.

## Fixed facts

REPLACE: record split and outcome counts, selected model, threshold, confusion counts, transformed-feature count, subgroup-row count, bootstrap replicates, and prediction-check status.

## Differences

REPLACE: describe every difference or write `none observed`. Do not silently replace reference evidence.
