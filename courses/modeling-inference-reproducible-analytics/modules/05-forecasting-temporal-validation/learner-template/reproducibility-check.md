# Reproducibility check

REPLACE: record environment, install, five upstream fingerprints, source row checks, rebuild command and target, 20 invariant results, output comparison, existing-target refusal, and validator result.

REPLACE: state whether any source, fold, model, or output changed. If yes, stop and document the return.
