# Environment note

REPLACE: Record operating system, architecture, shell, Python executable, Python version, NumPy, pandas, SciPy, statsmodels, R version, encoding, and line endings.

REPLACE: List exact setup, build, R, and validation commands.

REPLACE: Record any difference from the declared environment and its disposition.
