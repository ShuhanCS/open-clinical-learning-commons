# Reproducibility check

[REPLACE: Record date, OS, Python, SQLite, exact packages, repository commit, source archive fingerprint, ordered commands, output comparisons, validator result, second clean assembly, independent reproducer, and unresolved operating-system limits.]
