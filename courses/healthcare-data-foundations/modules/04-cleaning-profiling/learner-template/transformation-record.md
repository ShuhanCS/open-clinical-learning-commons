# Transformation record

## Inputs

[REPLACE: Record the accepted table identity and fingerprint.]

## Defect-layer build

[REPLACE: Record the ordered deterministic build without exposing accepted rows as manually editable source.]

## Profiling and resolution

[REPLACE: Record field profiling, rule detection, risk classification, resolution, and final fingerprint checks.]

## Exclusions and corrections

[REPLACE: State every correction or exclusion rule. If none were used beyond deterministic restoration, say so.]
