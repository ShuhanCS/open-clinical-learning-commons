# APP-4 Checkpoint 02: Workflow, sandbox, safety, and monitoring release

## 1. Checkpoint identity and place in the course

- Checkpoint ID: `oclc-app4-cp02`.
- Course: APP-4, Data for Clinical Decision Support.
- Due point: end of instructional Week 6.
- Course points: 25.
- Point source: Module 04's 25 points counted once.
- Module 05 points: 0.
- Module 06 points: 0.
- Checkpoint version: `0.1.0`.
- Commons release: `0.84.0`.
- Package: `courses/clinical-decision-support/checkpoints/02-workflow-sandbox-safety-release/`.
- Decision: is the complete nonproduction workflow, prototype, failure, safety, monitoring, governance, and embedded-ML case strong enough for clinician leadership review?

The checkpoint freezes the complete accepted Module 04, Module 05, and Module 06 reference workspaces. It does not recompute evidence, repair a result, change a decision, or make the case more favorable. Its job is to prove chain of custody, preserve all failures and unavailable states, count the 25 points once, and give Module 07 the exact record it must interpret.

Checkpoint acceptance does not authorize real-patient scoring, clinical threshold acceptance, clinical alerting, clinical action, silent-mode evaluation, implementation, production connection, or deployment.

## 2. Decision, readers, and required answer

The primary readers are the APP-4 faculty owner, Joe Joseph, MD, the fictional `CGH-GIM-01` clinical decision support governance council, a clinical and patient-safety reviewer, a workflow and human-factors reviewer, an interoperability reviewer, a model-risk and biostatistics reviewer, an equity and accessibility reviewer, a privacy and security reviewer, a responsible-AI reviewer, and an independent reproducer.

The checkpoint must answer:

1. which workflow moment, user, action, nonaction, and patient consequence were retained;
2. why `panel-t003` was selected for sandbox mechanics;
3. why `0.03000000` is still unaccepted and why no threshold is accepted;
4. how the 25-point score is counted once;
5. whether all 31 sandbox cases and traces remain exact;
6. which visible and silent failures remain material;
7. why the malformed-card accessibility result remains blocked;
8. whether the safety case covers every inherited and prospective hazard;
9. how silent failure is detected independently;
10. which monitoring measures, thresholds, owners, and unavailable states apply;
11. who may investigate, pause, stop, fall back, restart, or retire the concept;
12. whether the ML challenger used the same evidence and untouched evaluations;
13. which replacement rules failed and why the transparent model remains retained;
14. how all 82 noncompensable gates are counted; and
15. what Module 07 may and may not decide.

The supported reference progression is `continue with conditions`. The curriculum record is complete enough for clinician leadership interpretation and defense. The design and threshold remain mechanics fixtures, the transparent model remains retained, and all clinical and production authority remains absent.

## 3. Accepted Module 04 workflow, burden, human-factors, and equity package

The assembler creates the exact `oclc-app4-04` version `0.1.0` reference workspace from Commons `0.81.0`.

| Identity item | Accepted value |
|---|---:|
| Assembled files | 302 |
| Immutable manifest rows | 285 |
| Manifest bytes | 60,302 |
| Manifest SHA-256 | `41692b01fa2c339068fcdbf5fbc6f3e301a79ba4535d9ecb94d602cb2e4b3bf9` |
| Course points | 25 |
| Gates | 20 of 20 |

The checkpoint retains:

- the full 263-file Week 3 checkpoint inside the Module 04 chain;
- 1,000 synthetic people and 1,200 synthetic encounter opportunities;
- 288 candidate-frame encounters and 39 input-unavailable encounters;
- 7,200 candidate event rows and 108 equity rows;
- all six unaccepted evidence candidates;
- the rejected Module 02 `0.20` mechanics fixture;
- candidate card counts of 116, 12, 3, 3, 0, and 0;
- workflow task analysis, role and handoff mapping, timing, interruption, and hidden-work review;
- burden, repeat, unavailable-input, and no-alert comparisons;
- usability, automation-bias, override, stop, access, equity, privacy, language, disability, and patient-communication review;
- `panel-t003` as the passive contextual panel fixture for mechanics only;
- `0.03000000` as an unaccepted sandbox fixture;
- accepted clinical threshold `none`;
- progression `continue with conditions`; and
- 25.00 of 25.00 points carried into Week 6 exactly once.

Module 04's 20 gates remain required. The checkpoint may not convert synthetic interaction evidence into observed burden or behavior, use an unsupported subgroup rate, target a group, accept a threshold, or turn the panel into a clinical recommendation.

## 4. Accepted Module 05 sandbox prototype and failure package

The assembler creates the exact `oclc-app4-05` version `0.1.0` reference workspace from Commons `0.82.0`.

| Identity item | Accepted value |
|---|---:|
| Assembled files | 341 |
| Immutable manifest rows | 324 |
| Manifest bytes | 75,019 |
| Manifest SHA-256 | `6bc3e7c0040b8ae93d273d1464459ae8d500913e0e8a423ca1e5b120256c8baf` |
| Course points | 0 |
| Gates | 20 of 20 |

The checkpoint retains:

- the complete nested 302-file Module 04 workspace;
- local, nonnetworked runtime with no listener, client, FHIR server, or external Python dependency;
- FHIR R4-shaped resources and CDS Hooks-shaped messages disclosed as teaching shapes;
- 31 declared cases;
- 12 Module 04 positive fixtures and one repeat-positive case;
- 184 prefetch resources, 31 responses, and 61 trace events;
- normal, exact-boundary, repeat, missing, stale, inconsistent, delayed, terminology, hook-version, model-version, service-unavailable, timeout, visible-failure, silent-failure, and accessibility routes;
- all 31 tests passing their declared expected behavior;
- all 17 failure modes remaining visible;
- no-card states retaining a trace reason rather than implying low risk;
- one received request with no response, terminal trace, or human notice detected through independent reconciliation;
- one malformed-card accessibility defect blocked from release;
- no suggestion, order, link, automatic action, or live-system route;
- Module 04's 25.00 points carried once; and
- Module 05 adding zero points.

A passing teaching fixture does not establish interoperability, conformance, usability, safety, clinical validity, or utility. The checkpoint may not hide a failure, infer a missing resource, silently convert a unit, interpret no card as favorable, waive the accessibility blocker, or claim that the prototype is ready for clinical testing.

## 5. Accepted Module 06 safety, monitoring, governance, and embedded-ML package

The assembler creates the exact `oclc-app4-06` version `0.1.0` reference workspace from Commons `0.83.0`.

| Identity item | Accepted value |
|---|---:|
| Assembled files | 387 |
| Immutable manifest rows | 369 |
| Manifest bytes | 88,971 |
| Manifest SHA-256 | `e6553079256fdd2a37ab042a87c2ec69812cad7074abefa7d7907e6ee7b56f7d` |
| Course points | 0 |
| Gates | 22 of 22 |

The checkpoint retains:

- the complete nested 341-file Module 05 workspace and its 302-file Module 04 chain;
- all 17 inherited failure modes;
- the four-ledger silent-failure finding;
- the blocked accessibility defect;
- 22 hazards;
- 20 monitoring measures;
- eight seeded monitoring scenarios;
- 12 human-owned escalation routes;
- zero automatic clinical actions;
- detection, control, ownership, escalation, fallback, stop, restart, and retirement routes;
- one fixed `GradientBoostingClassifier` challenger without tuning;
- 7,544 common prediction rows and three common predictors;
- the same development, temporal-holdout, and transport-stress splits;
- the same weights, missing-input rules, threshold candidates, alert budget, and evaluation rows;
- all leakage and environment checks passing;
- all 11 predeclared replacement rules;
- eight replacement rules passing;
- R03 failing with temporal-holdout AUC difference `-0.00743486`;
- R04 failing with transport-stress AUC difference `-0.01928938`;
- R08 failing with worst supported subgroup AUC degradation `0.10385240`;
- final decision `retain transparent model`; and
- Module 06 adding zero points.

The challenger may improve some measures and still fail replacement. Replacement is conjunctive: all 11 rules must pass. The checkpoint may not round a failure away, move a rule after seeing results, retune the model, add another challenger, choose a threshold, or treat feature importance as cause.

## 6. Point architecture and no-double-counting rule

| Source | Accepted points | Checkpoint treatment |
|---|---:|---|
| Module 04 workflow, burden, human factors, and equity | 25.00 | ten source criteria counted once |
| Module 05 sandbox and failure modes | 0.00 | required gate |
| Module 06 safety, monitoring, governance, and ML | 0.00 | required gate |
| Checkpoint integrity | 0.00 | required gate |
| Cumulative Week 6 package | 25.00 | one summary, not a second score |

`checkpoint-score.csv` carries the ten Module 04 criteria with their exact possible and earned points. The ten earned values sum to 25.00. Module 05 and Module 06 each have a zero-point gate row. One summary row reports 25.00 of 25.00 and is explicitly non-additive.

A duplicated total, a new prototype score, a safety score, an ML score, or points assigned to Module 05 or Module 06 fail the release. The Week 6 checkpoint remains separate from the accepted 40-point Week 3 checkpoint and future 35-point official-end-date package. Course points remain `40 + 25 + 35 = 100`.

## 7. Cumulative evidence index and chain of custody

`evidence-index.csv` contains three ordered module rows. Each row records title, version, Commons release, assembled files, nested manifest rows, bytes, SHA-256, checkpoint points, gate count, progression, accepted decision, and checkpoint role.

The assembler generates a 1,030-row `candidate-manifest.csv`. Each row contains:

- checkpoint-relative path;
- bytes;
- SHA-256;
- source module;
- source version; and
- role.

The accepted candidate manifest is 236,732 bytes with SHA-256 `14ac12dd890045dce21cdc44a9b614770b8b2428bd71a1d4f5eb9cc9de63d642`.

Candidate paths are under:

- `candidate/module-04/` with 302 files;
- `candidate/module-05/` with 341 files; and
- `candidate/module-06/` with 387 files.

Paths are sorted, relative, portable, unique, and free of traversal. Every row verifies actual bytes and hash. Every source module and version must match its declared route. Each nested `release-manifest.csv` must reproduce its exact row count, byte count, and SHA-256.

The learner and reference packages have the same 1,030 candidate files and the same candidate manifest. Only the nine checkpoint work records differ.

## 8. Responsible synthesis and claims contract

`responsible-claims-check.md` supplies the cumulative interpretation without creating a new analytic result.

The strongest supported checkpoint claim is that the nonproduction teaching package is complete enough for clinician leadership review. This rests on verified release identity and complete records for workflow, sandbox behavior, failure detection, safety, monitoring, governance, and the fixed ML comparison.

The synthesis must keep these distinctions explicit:

- selected mechanics fixture versus accepted clinical design;
- unaccepted threshold fixture versus accepted clinical threshold;
- synthetic workflow evidence versus observed local work;
- passing expected sandbox behavior versus interoperability or safety;
- a seeded failure versus a clinical event rate;
- a complete safety case versus proof of safety;
- a teaching trigger versus a validated control limit;
- historical public evidence versus local validity;
- prediction importance versus cause;
- curriculum progression versus clinical authority; and
- clinician recommendation versus permission to test or deploy.

The record must name human owners for faculty progression, clinical meaning, patient safety, workflow, interoperability, model risk, survey methods, equity, accessibility, privacy, security, governance, responsible AI, clinician leadership, and independent reproduction. No agent owns a clinical decision.

## 9. Failure, unavailable, uncertainty, and accessibility retention

The checkpoint must foreground evidence that could otherwise disappear during synthesis:

- all 17 inherited sandbox failure modes;
- missing, stale, inconsistent, delayed, terminology, version, service, and timeout routes;
- no-card outcomes with explicit trace reasons;
- the repeat-positive route and its suppression behavior;
- one silent failure absent from response, terminal-trace, and notice ledgers;
- the requirement to reconcile request, response, terminal-trace, and human-notice ledgers independently;
- one blocked malformed-card accessibility defect;
- all unavailable monitoring states;
- monitoring triggers that have no local clinical validation;
- temporal-holdout discrimination degradation;
- transport-stress discrimination degradation;
- supported subgroup degradation above the allowed rule;
- unsupported subgroup results that remain suppressed;
- the absence of an accepted threshold; and
- the absence of local utility evidence.

Unavailable is never converted to zero, favorable, safe, or not applicable without evidence. A visible failure may be a passing test when failure is the declared expected behavior. The accessibility defect remains blocked even though the defect-detection test passes.

## 10. Safety, monitoring, escalation, and governance readiness

Checkpoint readiness requires all 22 hazards to retain:

- source failure or prospective hazard;
- affected user or patient consequence;
- detection route;
- prevention or mitigation control;
- control owner;
- escalation route;
- safe fallback;
- stop condition;
- restart condition;
- retirement trigger; and
- residual authority limit.

All 20 monitoring measures retain exact units, numerator or value origin, denominator, window, cadence, direction, threshold, unavailable state, human response, owner, and claim limit. All eight seeded scenarios remain teaching fixtures. A monitoring value may be available, outside a teaching trigger, inside a teaching trigger, or unavailable. None of those states creates an automatic clinical action.

All 12 escalation routes remain human owned. They specify confirmation owner, decision owner, documentation route, safeguard, fallback, stop, restart, and retirement handling. Automatic actions total zero.

Silent-failure monitoring uses the request ledger as the denominator and joins response, terminal-trace, and notice ledgers independently by request identity. One or more reconciled silent failures triggers human review. If reconciliation is unavailable, the affected evaluation stops. The service log cannot substitute for independent reconciliation.

## 11. Embedded-ML comparison and replacement decision

The checkpoint preserves one fixed challenger comparison. It does not conduct a new model search.

The accepted comparison contract requires:

- the same public evidence release;
- the same historical target;
- the same three predictors;
- the same inclusion, exclusion, missing-input, and information-cutoff rules;
- the same development, temporal-holdout, and transport-stress rows;
- the same survey weights;
- the same candidate thresholds and alert budgets;
- one fixed gradient-boosted classifier;
- no tuning after holdout inspection;
- leakage checks before performance interpretation;
- discrimination, calibration, burden, subgroup, and transport review; and
- all 11 replacement rules to pass.

The 7,544 prediction rows remain common between the transparent and challenger models. Eight rules pass. Three fail:

| Rule | Exact result | Decision |
|---|---:|---|
| R03 | Temporal-holdout AUC difference `-0.00743486` | fail |
| R04 | Transport-stress AUC difference `-0.01928938` | fail |
| R08 | Worst supported subgroup AUC degradation `0.10385240` | fail |

The transparent model remains retained. This is not a finding that the transparent model is clinically valid or superior in every respect. It is the exact result of the predeclared replacement contract for this teaching comparison.

## 12. Noncompensable gates, learner records, and defense

The checkpoint carries:

- 20 of 20 Module 04 gates;
- 20 of 20 Module 05 gates;
- 22 of 22 Module 06 gates; and
- 20 of 20 checkpoint integrity gates.

All 82 gates are required. A numeric score cannot compensate for a failed gate.

The checkpoint integrity gates cover:

1. exact accepted module identities;
2. exact 1,030-row candidate manifest;
3. exact module file counts and nested manifests;
4. 25 points counted once;
5. all 62 inherited gates;
6. `panel-t003` fixture identity;
7. unaccepted `0.03000000` and no accepted threshold;
8. all 31 sandbox cases;
9. all 17 inherited failures;
10. independent silent-failure reconciliation;
11. blocked accessibility defect;
12. all 22 hazards;
13. all 20 measures;
14. all 12 human-owned escalation routes;
15. comparable fixed challenger evidence;
16. R03, R04, and R08 failures;
17. retained transparent model;
18. complete claims, reproduction, AI-use, and defense records;
19. protected Module 07 handoff; and
20. absent clinical and production authority.

The package has nine learner work records:

1. `README.md`;
2. `evidence-index.csv`;
3. `checkpoint-score.csv`;
4. `checkpoint-gates.csv`;
5. `responsible-claims-check.md`;
6. `reproducibility-check.md`;
7. `ai-use.md`;
8. `checkpoint-defense.md`; and
9. `module07-handoff.md`.

Every learner record has a visible placeholder. Every reference record is complete. Learner-facing prose uses plain ASCII and portable relative paths.

The defense has 14 ordered questions. Every answer includes an exact answer, evidence path, decision consequence, and limit. The questions cover frozen scope, point counting, gates, design and threshold, sandbox meaning, retained failures, safety, silent and unavailable monitoring, ownership, fair ML comparison, failed rules, exact ML results, Module 07 permission, and absent authority.

## 13. Deterministic assembly contract

`build_checkpoint.py` imports the three accepted module workspace builders. It creates complete reference workspaces in a temporary directory, verifies each expected file count, and copies every file into the candidate tree while computing the outer manifest.

| File class | Count |
|---|---:|
| Immutable checkpoint controls | 7 |
| Checkpoint work records | 9 |
| Candidate manifest | 1 |
| Module 04 candidate files | 302 |
| Module 05 candidate files | 341 |
| Module 06 candidate files | 387 |
| Total assembled files | 1,047 |

The checkpoint uses only the Python standard library. The assembler refuses any existing destination. It builds reference releases sequentially during the self-check to keep temporary disk use bounded. Two clean reference assemblies must produce the same report and byte-identical candidate tree. The learner assembly must reproduce the same candidate manifest. Reference claims contain no placeholder, while each learner work record contains one.

## 14. Validation, copied execution, and mutation rejection

`validate_checkpoint.py` checks:

- exact 1,047-file package size;
- exact checkpoint identity and Commons release;
- exact 1,030 sorted candidate rows;
- candidate-manifest bytes and SHA-256;
- portable, unique, traversal-free paths;
- every candidate byte count and SHA-256;
- source module, version, and role on every manifest row;
- exact 302, 341, and 387 module file counts;
- all three nested manifest identities;
- source release identities and Commons versions;
- all 62 inherited gates;
- exact evidence-index point, gate, and file maps;
- ten Module 04 score criteria totaling 25.00 once;
- zero points for Modules 05 and 06;
- one non-additive 25.00 summary;
- all 20 checkpoint gates and their owners;
- design, threshold, failure, accessibility, safety, monitoring, ML, and authority claims;
- complete source hashes and reproduction boundary;
- complete accountable AI-use record;
- 14 complete defense answers;
- exact clinician-leadership handoff; and
- complete learner placeholders or complete reference records, as appropriate.

The validator runs from the repository and from a copied assembled package. Reference validation passes 8,353 checks. Learner validation passes 8,308 checks. Copied validation passes. Twenty-four failure routes are rejected, including candidate mutation, manifest mutation, missing candidate evidence, changed points, duplicate score, failed gate, accepted threshold, expanded authority, hidden failure, waived accessibility blocker, changed replacement rules, altered model decision, incomplete claims, incomplete defense, invalid progression, and a learner starter submitted as complete.

## 15. Common failure modes and instructor response

| Failure | Why it fails | Instructor response |
|---|---|---|
| Recompute a source module | breaks accepted release identity | restore the exact candidate package |
| Count the Module 04 total twice | changes the 25-point contract | restore the ten criteria and one non-additive summary |
| Give Module 05 or Module 06 points | converts gates into new weight | restore the `25 / 0 / 0` map |
| Treat `0.03000000` as accepted | turns a synthetic case fixture into a clinical decision | restore unaccepted status and accepted threshold none |
| Call all 31 cases successful alerts | ignores that some expected passes are visible stops | state the declared behavior for each route |
| Treat no card as low risk | hides suppression or failure | retain the exact trace reason |
| Use the service log to detect silence | the seeded event disappears after receipt | reconcile all four independent ledgers |
| Waive the malformed-card defect | removes an accessibility release blocker | retain the blocked defect |
| Turn unavailable into zero | invents favorable evidence | restore unavailable and its stop route |
| Accept the challenger because eight rules pass | replacement requires all 11 | retain R03, R04, R08, and the transparent model |
| Round a negative AUC difference to no change | changes a predeclared decision after results | retain eight-decimal results |
| Treat subgroup degradation as a group trait | exceeds support and invites harmful action | restore support limits and prohibit targeting |
| Call teaching shapes FHIR conformance | no conformance review occurred | restore teaching-shape disclosure |
| Grant silent-mode evaluation | checkpoint scope is curriculum assembly | restore all authority prohibitions |

The instructor rewards faithful synthesis and accountable judgment, not a more favorable clinical story.

## 16. Progression contract and Module 07 handoff

Allowed progression values are `continue`, `continue with conditions`, `revise`, and `refer`.

The supported reference progression is `continue with conditions`. Module 07 is permitted for clinician leadership review, recommendation, and defense when:

- all 1,030 candidate files verify;
- all three nested manifests verify;
- the Week 6 score is 25.00 of 25.00 once;
- all 62 inherited and 20 checkpoint gates pass;
- `panel-t003` remains a passive mechanics fixture;
- `0.03000000` remains unaccepted and accepted threshold remains none;
- all 17 failures, the silent failure, and the blocked accessibility defect remain visible;
- all 22 hazards, 20 measures, and 12 human-owned escalation routes remain complete;
- R03, R04, and R08 remain failed;
- the transparent model remains retained; and
- all clinical and production authority remains absent.

Joe Joseph, MD, is the designated clinician leader under the established identity boundary. Direct confirmation is required for participation, schedule, format, recording permission, and final biography wording before alpha. The course must not state a current employer or current title without a dated verified source.

Module 07 may interpret the frozen record, surface patient and staff consequences, assign owners, state uncertainty, identify who may pause or stop the concept, define evidence for reconsideration, recommend a bounded progression disposition, and defend it. It may not change an upstream byte, recompute evidence, repair the frozen candidate, retune a model, accept a threshold, score a patient, begin silent mode, alert or act clinically, implement, connect production, or deploy.

## 17. Release, review, and exit criteria

The checkpoint is a runnable release candidate when:

- this 17-section specification is complete;
- the builder and validator self-checks pass;
- two reference assemblies have byte-identical candidates;
- the learner package validates structurally;
- copied validation passes;
- all 24 deliberate failure routes are rejected;
- the 236,732-byte candidate manifest identity is recorded in the contract, release, evidence, reproduction, course, ledger, and curriculum checker;
- Commons is advanced to `0.84.0`;
- catalog and course references identify Checkpoint 02 as complete;
- Git records one scoped commit; and
- the feature branch is pushed and remote verified.

Human review remains required before alpha for the clinical purpose, workflow claims, FHIR and CDS Hooks teaching shapes, failure fixtures, accessibility blocker, safety case, monitoring triggers, silent-failure route, governance ownership, ML contract and decision, subgroup language, privacy and security boundaries, Joe Joseph participation details, responsible-AI record, and independent reproduction.

The next durable unit is APP-4 Module 07, clinician leadership, recommendation, and defense.
